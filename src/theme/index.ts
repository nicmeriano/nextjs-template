import { extendTheme } from '@chakra-ui/react';
import {
    typography,
    borders,
    breakpoints,
    colors,
    icons,
    opacity,
    radius,
    shadows,
    spacing,
    transitions,
    zIndex
} from './base';

export const theme = extendTheme({
    borders,
    breakpoints,
    colors,
    icons,
    opacity,
    radius,
    shadows,
    spacing,
    transitions,
    typography,
    zIndex
});
