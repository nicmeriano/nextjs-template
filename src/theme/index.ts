import {
    typography,
    borders,
    breakpoints,
    colors,
    icons,
    opacity,
    radius,
    shadows,
    spacing,
    transitions,
    zIndex,
} from './base';

export const theme = {
    borders,
    breakpoints,
    colors,
    icons,
    opacity,
    radius,
    shadows,
    spacing,
    transitions,
    typography,
    zIndex,
};
