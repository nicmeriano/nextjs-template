export type ZIndex = typeof zIndex;

export const zIndex = {
    back: -1,
    auto: 'auto',
    base: 0,
    forward: 5,
    dropdown: 10,
    sticky: 30,
    modal: 40,
    toaster: 50,
    tooltip: 60,
};
